{{- if .Values.ingress.enabled }}
# API ingress: strip /api prefix before proxying to api-express (rewrite applies only here).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "vahanplus.fullname" . }}-api
  labels:
    {{- include "vahanplus.labels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.host }}
      secretName: {{ .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /api(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "vahanplus.fullname" . }}-api-express
                port:
                  number: {{ .Values.apiExpress.port }}
          {{- if .Values.apiNest.enabled }}
          - path: /v2
            pathType: Prefix
            backend:
              service:
                name: {{ include "vahanplus.fullname" . }}-api-nest
                port:
                  number: {{ .Values.apiNest.port }}
          {{- end }}
---
# Web ingress: no rewrite — required so /_next/static/* serves real CSS/JS (not HTML).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "vahanplus.fullname" . }}-web
  labels:
    {{- include "vahanplus.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.host }}
      secretName: {{ .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "vahanplus.fullname" . }}-web
                port:
                  number: {{ .Values.web.port }}
{{- end }}
