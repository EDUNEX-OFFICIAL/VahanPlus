global:
  imagePullPolicy: IfNotPresent

apiExpress:
  replicaCount: 1
  image: vahanplus-api-express:latest

web:
  replicaCount: 1
  image: vahanplus-web:latest

worker:
  replicaCount: 1
  image: vahanplus-worker:latest

ingress:
  enabled: true
  host: vahanplus.staging.local
  className: nginx
  tls:
    enabled: false

monitoring:
  serviceMonitor:
    enabled: false

# Staging uses in-cluster or dev secrets from values.yaml defaults.
secrets:
  existingSecret: ""
