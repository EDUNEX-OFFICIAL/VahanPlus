global:
  imagePullPolicy: Always
  imageRegistry: ghcr.io/your-org

apiExpress:
  replicaCount: 2
  image: vahanplus-api-express:latest

web:
  replicaCount: 2
  image: vahanplus-web:latest

worker:
  replicaCount: 1
  image: vahanplus-worker:latest
  browserPoolStub: false

postgresql:
  host: your-rds-hostname.region.rds.amazonaws.com
  port: 5432

redis:
  host: your-elasticache-hostname.cache.amazonaws.com
  port: 6379

hpa:
  enabled: true

networkPolicy:
  enabled: true

ingress:
  enabled: true
  host: vahanplus.example.com
  className: nginx
  tls:
    enabled: true
    secretName: vahanplus-tls

monitoring:
  serviceMonitor:
    enabled: true

secrets:
  existingSecret: vahanplus-app-secrets
