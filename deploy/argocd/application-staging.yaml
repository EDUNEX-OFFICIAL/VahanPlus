# Staging Argo CD Application — update spec.source.repoURL to your Git remote.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vahanplus-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/your-org/VahanPlus.git
    targetRevision: main
    path: deploy/helm/vahanplus
    helm:
      valueFiles:
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: vahanplus-staging
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
